# TileSim 总览：技能审查与定向优化

日期：2026-09-07。使用 `tilesim-visual-design` 的 Page visual review 模式，范围为总览页面，不再次重构全站。

## 环境与基线

- 用户页面为 `4173/overview?run=run-20260904-124802-ab027472`。本轮开始时 4173 与 5173 均拒绝连接，因此不能沿用上一轮的“联机正常”判断。
- 仅恢复前端 Vite 预览，并保留其持久终端会话。未启动、停止、重启或修改后端、Bridge 实现和发布服务。
- 在持久 Node REPL 中复用项目已有 Playwright 浏览器、context 和 page；没有修改全局配置、安装包或新增依赖。终端回归沿用仓库既有测试命令。
- 改动前检查 git status、分支、相关 diff 与源码；已有未提交改动保持原状。相关文件原文（包括此前未跟踪的样式、翻译和测试）存于 `runtime/visual-review/batch6/baseline/`，备份均使用 snapshot 扩展名。
- QA 清单先于实施创建于 `runtime/visual-review/batch6/qa-inventory.json`。基线为内置示例，1440/1100/1920 × 浅深主题，共 6 张，不将这些图称为原运行数据。

## 审查与方案

| 优先级   | 实际问题                                                   | 处理                                                             |
| -------- | ---------------------------------------------------------- | ---------------------------------------------------------------- |
| P2       | 1440×900 示例态中，网络区从 y≈865 才开始，首屏看不到域数据 | 保留深色主指标区但压缩重复标题、内边距、统计区间距；网络区上移   |
| P2       | 执行和指标入口在页面内部重复，右侧分析路径占用对照宽度     | 合并为摘要内同一导航区，保留执行、指标、可信度三个入口；不改路由 |
| P2       | 网络预览只有占用，已有等待字段需要跳页查看                 | 使用完整宽度的原生表格显示域、利用率、队列延迟和拥塞延迟         |
| 验证限制 | 后端 5173 不可达，无法复核用户原运行                       | 采用明确标识的内置示例与经 UI 导入的测试夹具；不伪装联机验收     |

方案在编辑前已输出。未在已检查状态中发现本批新增 P0/P1 阻塞问题；这不构成对离线后端或未验证联机路径的保证。

视觉取舍遵循技能清单：先解决内容可见性、重复入口与对照效率，再调整比例。保留上一批的克制实色，不添加图片、品牌资产、装饰图形或动效；本轮不是新的参考驱动大改版。

## 实施与数据规则

- `src/views/OverviewView.vue`：摘要标题去重，分析入口合并，占用列表改为原生表格；每次仍最多展示报告顺序中的前 6 个域。
- `src/styles/workbench.css`：清理旧双栏列表和侧边导航样式，新增总览专属紧凑比例与表格规则；网络页的主观测区规则不变。
- `src/i18n/workstreams/workbench-review.ts`：新增“通信域观测”和延迟解读边界的中英文文案。
- `tests/components/workbench-review.test.js`：保留既有边界测试，同步列表到表格的选择器；新增原始延迟、单位、零值/缺失和单一入口检查。
- `tests/e2e/dashboard.spec.js`：保留已有长标识、6 行上限、零宽和网络证据检查，新增首屏可见性与三个分析入口的键盘往返测试。

新增列直接使用 `queue_delay_us` 和 `congestion_delay_us`，µs 在列头明确标示；不相加为请求耗时、不推测因果、不根据占用重排瓶颈。0 仍是 0，缺失仍是破折号。`UtilizationMeasure` 不变，越界值不绘条的规则保持。Trace provenance、校准、精度、原始工件与证据范围不变。

## 独立视觉验收

对同一示例、同一主题和宽度比较；数值为浏览器 CSS 像素，未滚动的 900px 高首屏：

| 宽度 | 网络区修改前顶部 | 修改后顶部 | 修改后第二行底部 |
| ---- | ---------------- | ---------- | ---------------- |
| 1440 | 865.4            | 711.3      | 875.5            |
| 1100 | 853.2            | 734.4      | 898.6            |
| 1920 | 875.8            | 715.3      | 879.5            |

1440px 摘要由 339.0px 收紧到 243.5px。浅深两主题均验证两行示例数据进入首屏，无页面级横向溢出，axe 无违规。不是要求任意长内容或展开证据后全页不滚动。

另外通过真实 UI 导入仓库已有测试夹具，检查：英文证据展开/收起、1100px 英文表头宽度、失败状态、缺失域空状态、40 个长标识只显示前 6 条、零值和缺失区别。错误 JSON 导入出现明确错误提示且不清除先前有效内容；没有浏览器 pageerror。

截图与数据：`runtime/visual-review/batch6/before/`、`after/`；矩阵布局与 axe 结果见各目录 `layout.json`，探索性检查见 `after/exploratory.json`。对比图 `overview-comparison.png` 使用同一内置示例的前 900px，左旧右新，不能用来证明真实系统性能。

## 验证与性能

- contracts:check、deps:check、typecheck、lint、format:check、生产构建、git diff --check 通过。
- 单元/组件：50 个文件，307 项通过，含既有 ECharts 异步卸载测试。
- E2E：41 项通过、5 项 live deployment 测试按配置跳过；未删除断言或更新 golden 图片。
- WSL Python 编译与前端仓库 Bridge 隔离单测：77 项通过，不连接用户后端运行服务。
- 日志：`runtime/visual-review/batch6/gates.log`、`e2e.log`、`bridge.log`。本批独立 patch：`runtime/visual-review/batch6/batch6-only.patch`。
- 总览 JS 8.94→8.81 kB（gzip 3.79→3.86），共享工作区 CSS 7.06→7.36 kB（gzip 1.70→1.82）；主入口 851.67→851.87 kB（gzip 193.02→193.11），主样式和 ECharts runtime/renderer 不变。
- 不新增请求、图表实例、观察器、定时器、动画或依赖。总览已有不加载图表模块的回归继续通过；未做新的帧率压力基准。

## 未解决与交付边界

原运行的联机加载、后端重连和真实工件链接没有在本轮复核；需要 5173 服务恢复后再验收。既有主入口大于 500kB 告警和 schema date-time 提示未隐藏。英文界面的既有侧栏分组和导入组合显示名仍有局部中文，本批只覆盖新增文案。

未部署到 5173、未提交、未暂存。前端源码预览继续运行，独立 QA 浏览器在检查完成后关闭。后续优先复核后端恢复后的同一真实运行，而非继续增加视觉元素。
