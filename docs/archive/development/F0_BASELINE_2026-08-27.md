# F0 前端基线

> 历史快照：本文件只记录 2026-08-27 的 F0 状态。当前状态和下一步以 `../AI_HANDOFF.md` 为准。

**状态**：validated  
**时间**：2026-08-27  
**机器记录**：[f0_baseline_2026-08-27.json](f0_baseline_2026-08-27.json)

## 1. 基线身份

- 前端：`D:\tileSim-web`，`main@c7cad28`，既有 Week 6 工作树保持不变。
- 后端：`D:\tileSim-week6`，`codex/week6-design-space-closure@e06cab9`。
- 部署：`local_worktree_snapshot`。
- source/build revision：一致。
- source/build state digest：`3cb7e2ab72fb8e7a688a9a711ba1070be5f70193be13cde8356d5f70cb7278a1`，一致。
- bridge：`execution_ready=true`。

F0 不声明当前未提交文件的所有权，也不清理、提交或重新组织它们。完整的 F0 前工作树路径记录在机器清单中。

## 2. 样本运行

固定样本为 `run-20260825-221201-d892ba16`。

| 项目            | 值                                        |
| --------------- | ----------------------------------------- |
| 进程状态        | `completed`                               |
| 报告状态        | `partial`，表示证据范围受限，不是运行失败 |
| 边界            | `S1->S6`，S7 hosted                       |
| 来源            | `synthetic_trace`                         |
| 校准            | `uncalibrated`                            |
| validation lane | `synthetic_consistency`                   |
| claim scope     | `exploratory`                             |
| 设计空间        | `S6_only` / `exploratory_s6_only`         |
| 请求            | 3/3 完成                                  |
| 吞吐            | 102.64 req/s                              |
| Runtime events  | 137                                       |
| Fabric records  | 1,164                                     |
| S7 stages       | 5                                         |
| 设计候选        | 3 个，1 个晋升 DES                        |

所有输入和输出文件的字节数与 SHA-256 记录在机器清单中。该运行只能支持合成一致性和探索性比较，不能用于 held-out、真实系统精度、全栈 Cycle 或 GPU-in-loop 声明。

## 3. 页面字段基线

| 页面/实体 | 主要来源                                                                      |
| --------- | ----------------------------------------------------------------------------- |
| 运行概览  | `run-result.summary`、`metrics.summary`、`run-result.bottleneck_report`       |
| S0        | `validation.trace_provenance`、`validation.resolution_entries`                |
| S1        | `input-runtime-trace`、`run-result.summary`、`metrics.request_metrics`        |
| S2        | `validation.resolution_entries`、`run-result.multi_granularity_profile`       |
| S3        | `metrics.system_summary.phase_fabric_contributions[*].memory_*`               |
| S4        | `metrics.system_summary.phase_fabric_contributions[*].device_*`               |
| S5        | `metrics.resource_convergence`、`phase_fabric_contributions[*].collective_id` |
| S6        | `metrics.system_summary`、`request_fabric_contributions`                      |
| S7        | `execution-envelope.stages`                                                   |
| S8        | `validation`                                                                  |
| S9        | `metrics`、`tail-cause-chain`                                                 |
| 设计空间  | `design-space.ranking`、`analytical_vs_des_disagreements`                     |

当前前端派生仅包括展示级去重、分组、求和和单位换算；详细规则继续以 `docs/WEEK6_RESULTS_UI.md` 为准。F1 必须把本表扩展为机器可读的 `UI field -> artifact -> JSON Pointer -> unit -> derivation` 审计。

## 4. 验证结果

| 门禁                  | 结果                              |
| --------------------- | --------------------------------- |
| Vitest                | 3 files，14/14 tests，通过        |
| bridge unittest       | 8/8，通过                         |
| Vite production build | 1,813 modules，通过               |
| 桌面 1440 px          | `scrollWidth=1440`，无横向溢出    |
| 移动 390 px           | `scrollWidth=390`，无横向溢出     |
| 控制台                | 0 errors                          |
| 分层识别              | 7 个 S0-S6 节点，5 个 S7 stages   |
| 完整 JSON 搜索        | `device_latency_us` 匹配 1,164 行 |

视觉证据保存在 `runtime/`，其 SHA-256 记录在机器清单中。`runtime/` 仍是本地运行资产，不进入 Git。

## 5. 性能基线

测量环境：Microsoft Edge，Playwright Chromium，1440x1100，本地 bridge 已启动。

| 项目                         |         基线 |
| ---------------------------- | -----------: |
| 首次 DOMContentLoaded        |    117.57 ms |
| 恢复样本运行至分层结果可用   | 11,562.32 ms |
| 1.2 MB metrics：fetch 中位数 |      99.6 ms |
| JSON parse 中位数            |       9.2 ms |
| pretty stringify 中位数      |       4.3 ms |
| 1,164 行搜索中位数           |       3.5 ms |
| artifact 全流程中位数        |     116.6 ms |

`run_restore_ready_ms` 包含 bridge health、完整源码状态摘要和报告恢复，是当前最明显的启动等待基线。后续优化必须解释任何超过 20% 的回退；提升不能通过跳过版本或 state digest 检查获得。

## 6. 已知边界

- 网页执行只开放 `gpu_free`、`S1->S6`、built-in synthetic 和 strict S6 manifest。
- 当前构建未提供 GPU hardware、Verilator Cycle 或真实 ASTRA-Sim。
- 网页未开放显式 S6 Cycle hotspot request。
- browser cache 只保存视图/run ID/对比选择，不保存报告正文。
- F1 之前没有组件、Playwright CI、axe 或视觉回归门。

## 7. F0 结论

F0 验收完成：工作树、部署 identity、样本工件、字段来源、测试构建、视觉和性能均已登记。下一阶段是 F1 Fixture 与回归；F1 不得直接移动目录或引入 Router 重构。
